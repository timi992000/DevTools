<UserControl x:Class="DevTools.Views.DevGeneratorTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DevTools.Views" xmlns:generator="clr-namespace:DevTools.Views.Generator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TabControl>
            <TabItem Header="Guid Generator">
                <generator:DevGuidGeneratorView/>
            </TabItem>
            <TabItem Header="QR Code Generator">
            </TabItem>
            <TabItem Header="Lorem ipsum Generator">
                <generator:DevLoremIpsumGeneratorView/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
